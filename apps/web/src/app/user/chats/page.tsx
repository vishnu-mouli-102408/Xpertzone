import { UserChats } from "@/src/components";

const Page = () => {
  return (
    <div className="mx-auto min-h-[calc(100vh-64px)] w-full bg-black/50">
      <UserChats />
    </div>
  );
};

export default Page;
